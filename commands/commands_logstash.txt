// Dockerfile is used to build the image and install the logstash-output-mongo plugin only once and not repeat if container is ran again.
// In order to run the logstash container:

docker-compose up


// In order to shut of logstash container:

docker-compose down